<nav style="height:5vh;" class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path_for('home') }}">SchoolCRM</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li class="active">
                    <a href="{{ path_for('home') }}">Home</a>
                </li>
                {% if auth.check %}
                    <li class="dropdown">
                        <a href="#" style="padding:0.5vh;" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            <img style="width:3vw;height:4vh;marginmargin-right:0.25vw;" src="{{ base_url()}}/../public/images/users/{{ auth.user.id }}.jpg" class="responsive img-thumbnail" alt="Cinque Terre">
                            {{ auth.user.name }}
                            <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            {% if auth.role >= 2 %}
                                <li>
                                    <a href="{{ path_for('auth.course_create') }}">Create Course</a>
                                </li>
                                <li>
                                    <a href="{{ path_for('auth.user_create') }}">Create User</a>
                                </li>
                            {% endif %}
                            <li>
                                <a href="{{ path_for('auth.password.change') }}">change password</a>
                            </li>
                            <li>
                                <a href="{{ path_for('auth.signout') }}">sign out</a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path_for('auth.signup') }}">Sign up</a>
                    </li>
                    <li>
                        <a href="{{ path_for('auth.signin') }}">Sign in</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>